/**
 * Default error message
 */
export const DEFAULT_ERROR_MESSAGE = "an error occurred";

/**
 * List of error codes that are considered “normal” and will not trigger a log
 */
export const NORMAL_ERROR_CODES = [400, 401, 403, 404, 422];
